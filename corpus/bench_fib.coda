-- Tail-recursive fibonacci, 500k iterations.
-- int64 overflows intentionally — this stresses TCO arithmetic throughput.
fib = fix(\rec -> \a b n ->
  when (n == 0) is
    True  -> a
    False -> rec(b, a + b, n - 1)
)
fib(0, 1, 500000)
